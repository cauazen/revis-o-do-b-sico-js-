let area
let base = Number(prompt("Digite a base:"))
let altura = Number(prompt("Digite a base:"))
function calculaAreaTriangulo(b,a){
    return b * a / 2
}
alert(calculaAreaTriangulo(base, altura))